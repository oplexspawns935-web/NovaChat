[phases.build]
nixPkgs = ["python312", "python312Packages.pip"]
cmds = ["cd backend && pip install -r requirements.txt"]

[phases.start]
cmds = ["cd backend && uvicorn server:app --host 0.0.0.0 --port $PORT"]
